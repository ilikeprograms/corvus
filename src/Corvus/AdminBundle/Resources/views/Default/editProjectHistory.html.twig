{% extends 'CorvusAdminBundle:Default:adminBase.html.twig' %}

{% block breadcrumbs %}
	<li><a href="{{ path('admin_project_history') }}">Project History</a></li>
	<li class="active">Edit</li>
{% endblock %}

{% block mainContent %}
	<h2>Edit "{{ form.vars.value.projectName }} - {{ form.vars.value.role }}"</h2>
	<p class="well">
        Use this form to update the <b>Project overview.</b>
	</p>

	<form role="form" action="{{ path('admin_project_history_edit', {'id' : form.vars.value.id }) }}" method="post" {{ form_enctype(form) }}>
		<fieldset>
			<legend>Project History Fields:</legend>			 

            <div class="form-group">
                {{ form_label(form.slug) }}
                {{ form_widget(form.slug) }}
            </div>

    		<div class="form-group">
                {{ form_label(form.project_name) }}
                {{ form_widget(form.project_name) }}
            </div>

            <div class="form-group">
                {{ form_label(form.project_description) }}
                {{ form_widget(form.project_description) }}
            </div>

            <div class="form-group">
                {{ form_label(form.role) }}
                {{ form_widget(form.role) }}
            </div>

            <div class="form-group">
                {{ form_label(form.process) }}
                {{ form_widget(form.process) }}
            </div>

            <div class="form-group">
                {{ form_label(form.feedback_received) }}
                {{ form_widget(form.feedback_received) }}
            </div>

            <div class="form-group">
                {{ form_label(form.reflection) }}
                {{ form_widget(form.reflection) }}
            </div>  

            <div class="form-group">
                {{ form_label(form.url) }}
                {{ form_widget(form.url) }}
            </div>

            <div class="form-group">
                {{ form_label(form.is_published) }}
                {{ form_widget(form.is_published) }}
            </div>
		</fieldset>

        <fieldset>
            <legend>Meta Info:</legend>

            <div class="form-group">
                {{ form_label(form.meta_title) }}
                {{ form_widget(form.meta_title) }}
            </div>

            <div class="form-group">
                {{ form_label(form.meta_description) }}
                {{ form_widget(form.meta_description) }}
            </div>
        </fieldset>

		{{ form_row(form._token) }}
        {{ core.formErrors(form) }}
		
		<input type="submit" class="btn btn-primary" value="Save" />
	</form>
	
	<aside>
		<h3>Help</h3>
		<p>Fields marked with <span class="required-hint"></span> are required</p>
	</aside>
{% endblock %}